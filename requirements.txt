# Mirrored from pyproject.toml for tools that still expect a requirements file.
# Prefer: `uv sync` (see README / docs/06-setup.md).
feedparser>=6.0.11
requests>=2.32.0
python-dotenv>=1.0.1
boto3>=1.35.0
psycopg[binary]>=3.2.0
