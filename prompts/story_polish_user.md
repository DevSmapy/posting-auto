오늘 날짜(Asia/Seoul): {{date}}
target_language: {{target_language}}
target_locale: {{target_locale}}

아래 translated story JSON을 요청 받은 대상 언어의 최종 카피로 다듬어라.
언어 위반이 있으면 대상 언어로 교정·재작성해도 된다. 다만 원본 사실은 모두 보존하고,
범위는 언어·문체·길이 조정으로 한정하며 새 정보 추가는 금지한다.

수정 포인트:
{{issues}}

```json
{{story_json}}
```
