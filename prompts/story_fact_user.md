오늘 날짜(Asia/Seoul): {{date}}

아래 기사 1건만 분석해 fact-layer JSON을 작성하라.
다른 이슈와 섞지 말고, 입력에 없는 사실은 추가하지 말라.

```json
{{article_json}}
```

visual_tags에 쓸 수 있는 ID (이 목록 밖의 값은 코드가 버린다):

{{visual_tag_options}}
